@using Sandbox;
@using Sandbox.UI;

@namespace MyGame
@inherits RootPanel
@attribute [StyleSheet]

<root>
    <ChatBox/>
    <VoiceList/>
    <AdminMenu />

</root>

@code
{
    public string name { get; set; } = "Noob";
    public string career { get; set; }
    public Pawn pawn { get; set; }
    public Hud()
    {
        Game.RootPanel = this;
        pawn = (Pawn)Game.LocalPawn;
        name = Game.UserName;
        //career = pawn.CurrentCareer.ToString();
    }
}