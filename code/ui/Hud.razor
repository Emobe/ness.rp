@using Sandbox;
@using Sandbox.UI;

@namespace MyGame
@inherits RootPanel
@attribute [StyleSheet]

<root>
    <ChatBox/>
    <VoiceList/>

    <div class="header">
        <!--<label>Shooter Game</label>
        <label class="subtitle">@name</label>
        <label class="subtitle">@career</label>-->
    </div>
</root>

@code
{
    public string name { get; set; } = "Noob";
    public string career { get; set; }
    public Pawn pawn { get; set; }
    public Hud()
    {
        pawn = (Pawn)Game.LocalPawn;
        name = Game.UserName;
        //career = pawn.CurrentCareer.ToString();
    }
}